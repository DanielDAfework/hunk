---
"hunkdiff": minor
---

Filter files by changed-line content: `/` now matches added and removed lines in addition to file paths, so you can find which files in a changeset touch a given string without leaving the review.
