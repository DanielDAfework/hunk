{
  "name": "agent-term",
  "version": "0.1.0",
  "description": "Agent-native terminal: PTY supervisor daemon exposed as an MCP server",
  "type": "module",
  "scripts": {
    "daemon": "bun run src/daemon.ts",
    "attach": "bun run src/attach.ts",
    "bench": "bun run bench/bench.ts",
    "typecheck": "bunx tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "bun-pty": "^0.4.10",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.9.0"
  }
}
